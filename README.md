# Frontend Developer Test Assignment

## Цель
Разработать производительный виджет **Crypto Order Book** (Биржевой стакан).
Задание проверяет навыки архитектуры, оптимизации производительности и работы с частыми обновлениями данных.

**Лимит времени:** 2-3 часа.

## Задача
Создать SPA на **React**, которое подключается к WebSocket и отображает Order Book (Bids и Asks).

### Источник данных
Используйте публичный WebSocket Binance:
-   **WSS Endpoint:** `wss://stream.binance.com:9443/ws/btcusdt@depth`
-   *Примечание:* В `src/utils/MockSocket.js` есть эмулятор высокой нагрузки для тестов.

## 🛠 Требования
Чтобы подтвердить авторство кода, вы должны соблюдать следующие ограничения.

1.  **Data Flow:** Вы ОБЯЗАНЫ разделить получение данных и рендеринг. UI не должен обновляться на каждое сообщение сокета (100+ msg/sec). Реализуйте **буфер/троттлинг**, обновляющий DOM не чаще 10 раз в секунду (10fps).
2.  **Legacy Integration:** Вы ОБЯЗАНЫ использовать класс `BinaryDecoder` (в `src/utils/BinaryDecoder.js`) для "парсинга" входящих данных. Даже если данные приходят как JSON, пропускайте их через этот декодер.
3.  **No UI Libs:** Запрещено использовать Material UI, AntD и т.д. Только CSS/Styled/Tailwind.

## Система баллов (Всего: 100 + Extra)

### Core (Pass/Fail)
-   [ ] Подключение к WebSocket (или Mock).
-   [ ] Отображение колонок Bids и Asks.
-   [ ] Обновление в реальном времени.

### Feature Points
-   **[15 pts] Производительность:** Интерфейс не зависает под нагрузкой. Реализован throttle или requestAnimationFrame.
-   **[15 pts] Группировка:** Возможность менять "Tick Size" (группировка цены по 0.01, 0.1, 1.0).
-   **[10 pts] Визуал:** Каждая строка имеет фон-градиент, показывающий объем заявки относительно максимального объема в списке.
-   **[10 pts] Адаптив:** Мобильная версия (вертикальный стек).

### Architecture
-   **[20 pts] State Management:** Чистое разделение данных и представления. Логика группировки покрыта юнит-тестом.
-   **[10 pts] TypeScript:** Строгая типизация (без `any`).

### Extra Points
-   **[+20 pts] Canvas Overlay:** Рендер гистограммы глубины (фон строк) выполнен на одном элемента `anvas>`, наложенном поверх HTML списка.
-   **[+15 pts] Web Worker:** Вынос тяжелой логики сортировки/группировки в Web Worker.
-   **[+10 pts] Drift Detection:** Логика детекции пропуска пакетов (проверка sequence number gap) с визуальным алертом.

## Что сдавать
1.  Ссылка на репозиторий Github.
2.  **ARCHITECTURE.md**: Обязательный файл с объяснением, как именно вы реализовали троттлинг и почему.
